// Injected by vite.config.ts `define`, for the MCP config snippets.

/** Absolute path of the Node binary that ran the dev server/build. */
declare const __MCP_NODE_PATH__: string
/** Absolute path of mcp/server.ts on this machine. */
declare const __MCP_SERVER_PATH__: string
